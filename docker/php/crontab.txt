# simple test
# * * * * * echo "some var $PWD" >> /var/www/app/logs/debug.log 2>&1

# check every minute - are there any "not taken" orders in db?
* * * * * /usr/local/bin/php /var/www/app/order-test.php >> /var/www/app/logs/cron-logs.log 2>&1

# daily rotate log files (every 1:00 am)
0 1 * * * /usr/sbin/logrotate /etc/logrotate.d/cron
0 1 * * * /usr/sbin/logrotate /etc/logrotate.d/menu
0 1 * * * /usr/sbin/logrotate /etc/logrotate.d/iiko-hook
